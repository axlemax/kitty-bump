import * as React from "react"
import { Typography } from "@material-ui/core"
import Page from "../layouts/Page"

const ProjectPage = (): JSX.Element => {
  return (
    <Page>
      <Typography variant="h3">{"Projects go here"}</Typography>
    </Page>
  )
}

export default ProjectPage
